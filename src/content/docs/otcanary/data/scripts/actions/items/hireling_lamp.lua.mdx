---
title: hireling_lamp.lua
description: Lâmpada para spawnar hireling na casa
---

import { Card, Aside } from '@astrojs/starlight/components';

## 🛠️ Informações do Arquivo

<Card title="hireling_lamp.lua" icon="star">
Script de ação Lua que implementa lâmpada mágica para spawnar hireling.
</Card>

<Aside type="tip">
**Caminho Original**: `data/scripts/actions/items/hireling_lamp.lua`

Lâmpada (ID via HIRELING_LAMP constant) que spawna hireling armazenado em custom attribute "Hireling". Validações: deve estar em casa (não container), não pode ser na porta, não pode haver outro hireling no mesmo tile, player deve ser dono da casa. Recupera hireling do DB via getHirelingById().
</Aside>

## 📄 Visão Geral do Código

**Resumo Executivo**

O script `hireling_lamp.lua` implementa lâmpada de spawning para hireling com validações robustas. Lâmpada armazena ID do hireling em custom attribute "Hireling". Ao usar, valida: posição está em casa (não container), não é porta, sem outro hireling no tile, player é dono. Recupera hireling do database, seta posição e spawna. Efeito TELEPORT ao aparição.

**Estrutura de Funcionalidades**

```
[Create hirelingLamp Action]
    ↓
[onUse function]
    ├─ spawnPosition = player:getPosition()
    ├─ hirelingId = item:getCustomAttribute("Hireling")
    ├─ house = getTile():getHouse() or nil
    ├─ Validações (qualquer falha = return false/true)
    │   ├─ If not house
    │   │   └─ "You may use this only inside a house."
    │   ├─ If on door
    │   │   └─ "You cannot spawn a hireling on the door"
    │   ├─ If another hireling here
    │   │   └─ "You cannot spawn another hireling here."
    │   ├─ If not owner
    │   │   └─ "You cannot spawn a hireling on another's person house."
    │   └─ continue if all pass
    ├─ hireling = getHirelingById(hirelingId)
    ├─ If not hireling
    │   ├─ Log error
    │   ├─ remove lamp
    │   └─ return true
    ├─ hireling:setPosition(spawnPosition)
    ├─ item:remove(1)
    ├─ hireling:spawn()
    ├─ spawnPosition:sendMagicEffect(TELEPORT)
    └─ return true
    ↓
[Register on HIRELING_LAMP (constant)]
```

## Variáveis e Constantes

| Nome | Tipo | Descrição |
|------|------|-----------|
| HIRELING_LAMP | constant | Item ID (lâmpada) |
| spawnPosition | position | Posição uso |
| hirelingId | integer | ID hireling custom attr |
| house | house obj | Casa atual (nil se container) |
| hireling | hireling obj | Hireling do DB |

## Funções

### hirelingLamp.onUse(player, item, fromPosition, target, toPosition, isHotkey)

Callback que spawna hireling.

**Parâmetros:**
- player: Creature player (casa dono)
- item: Lâmpada com custom attr
- fromPosition: Posição original
- target: Target opcional
- toPosition: Posição destino
- isHotkey: Boolean se via hotkey

## Validações (Sequencial)

### 1. Não em Container

```lua
if not house then
    player:getPosition():sendMagicEffect(CONST_ME_POFF)
    player:sendTextMessage(MESSAGE_FAILURE, "You may use this only inside a house.")
    return false
end
```

**Rejeita**: Uso em container/inventory.

### 2. Não na Porta

```lua
elseif house:getDoorIdByPosition(spawnPosition) then
    player:getPosition():sendMagicEffect(CONST_ME_POFF)
    player:sendTextMessage(MESSAGE_FAILURE, "You cannot spawn a hireling on the door")
    return false
end
```

**Rejeita**: Posição é porta da casa.

### 3. Sem Outro Hireling

```lua
elseif getHirelingByPosition(spawnPosition) then
    player:getPosition():sendMagicEffect(CONST_ME_POFF)
    player:sendTextMessage(MESSAGE_FAILURE, "You cannot spawn another hireling here.")
    return false
end
```

**Rejeita**: Já existe hireling no tile.

### 4. Player é Dono

```lua
elseif house:getOwnerGuid() ~= player:getGuid() then
    player:getPosition():sendMagicEffect(CONST_ME_POFF)
    player:sendTextMessage(MESSAGE_FAILURE, "You cannot spawn a hireling on another's person house.")
    return false
end
```

**Rejeita**: Player não é dono da casa.

## Fluxo Completo

```lua
function hirelingLamp.onUse(player, item, fromPosition, target, toPosition, isHotkey)
	local spawnPosition = player:getPosition()
	local hirelingId = item:getCustomAttribute("Hireling")
	local house = spawnPosition and spawnPosition:getTile() and spawnPosition:getTile():getHouse() or nil

	if not house then
		player:getPosition():sendMagicEffect(CONST_ME_POFF)
		player:sendTextMessage(MESSAGE_FAILURE, "You may use this only inside a house.")
		return false
	elseif house:getDoorIdByPosition(spawnPosition) then
		player:getPosition():sendMagicEffect(CONST_ME_POFF)
		player:sendTextMessage(MESSAGE_FAILURE, "You cannot spawn a hireling on the door")
		return false
	elseif getHirelingByPosition(spawnPosition) then
		player:getPosition():sendMagicEffect(CONST_ME_POFF)
		player:sendTextMessage(MESSAGE_FAILURE, "You cannot spawn another hireling here.")
		return false
	elseif house:getOwnerGuid() ~= player:getGuid() then
		player:getPosition():sendMagicEffect(CONST_ME_POFF)
		player:sendTextMessage(MESSAGE_FAILURE, "You cannot spawn a hireling on another's person house.")
		return false
	end

	local hireling = getHirelingById(hirelingId)
	if not hireling then
		player:sendTextMessage(MESSAGE_EVENT_ADVANCE, "There was an error creating the hireling and it has been deleted, please, contact server admin.")
		logger.warn("[hirelingLamp.onUse] Player {} is using hireling with id {} not exist in the database", player:getName(), hirelingId)
		logger.error("Deleted the lamp")
		item:remove(1)
		return true
	end

	hireling:setPosition(spawnPosition)
	item:remove(1)
	hireling:spawn()
	spawnPosition:sendMagicEffect(CONST_ME_TELEPORT)
	return true
end
```

## Constantes e Efeitos

| Nome | Tipo | Descrição |
|------|------|-----------|
| CONST_ME_POFF | constant | Efeito rejeição (smoke) |
| CONST_ME_TELEPORT | constant | Efeito spawn (teleport) |
| MESSAGE_FAILURE | constant | Tipo erro |
| MESSAGE_EVENT_ADVANCE | constant | Tipo evento |

## Mensagens de Erro

| Condição | Mensagem |
|----------|----------|
| Não em casa | You may use this only inside a house. |
| Na porta | You cannot spawn a hireling on the door |
| Outro hireling | You cannot spawn another hireling here. |
| Não dono | You cannot spawn a hireling on another's person house. |
| DB error | There was an error creating... (+ log) |

## Fluxo de Operação

```
[Player usa lampada em casa]
    ↓
[Validações (sequencial)]
    ├─ Em casa (house exists)?
    │   ├─ NÃO → POFF effect + msg + return false
    │   └─ SIM → continuar
    ├─ Não na porta?
    │   ├─ NÃO → POFF effect + msg + return false
    │   └─ SIM → continuar
    ├─ Sem outro hireling?
    │   ├─ NÃO → POFF effect + msg + return false
    │   └─ SIM → continuar
    ├─ Player é dono?
    │   ├─ NÃO → POFF effect + msg + return false
    │   └─ SIM → continuar
    └─ Todas passam → proceed
    ↓
[Fetch hireling do DB]
    ├─ Existe?
    │   ├─ NÃO → log error, remove lamp, return true
    │   └─ SIM → continuar
    └─ Proceed
    ↓
[hireling:setPosition(spawnPosition)]
    ↓
[item:remove(1)]
    ↓
[hireling:spawn()]
    ↓
[sendMagicEffect(TELEPORT)]
    ↓
[return true]
```

## Checkpoint de Aprendizagem

**Conceitos-chave:**
1. 4 validações sequenciais (early return)
2. Custom attribute "Hireling" storage
3. House owner checking (getOwnerGuid)
4. Door position rejection (getDoorIdByPosition)
5. Hireling position management
6. Database fetch + error handling
7. Logger warn/error para diagnostics

**Responsabilidades:**
1. Validar localização (em casa)
2. Validar posição (não porta)
3. Validar espaço (sem outro hireling)
4. Validar permissão (dono casa)
5. Fetch hireling do DB
6. Setar posição
7. Spawn com efeito

## 🤖 Informações da Documentação

<Card title="Metadata" icon="document">
**Tipo**: Script Lua (Action Handler - validações complexas)  
**Tamanho**: 55 linhas  
**Função**: 1 (onUse)  
**Item ID**: HIRELING_LAMP constant  
**Validações**: 4 (casa, porta, hireling, dono)  
**Efeitos**: 2 (POFF rejeição, TELEPORT spawn)  
**Storage**: Custom attribute "Hireling" (ID)
</Card>

<Aside type="note">
Lâmpada com 4 validações sequenciais (casa, porta, hireling, dono). Spawna hireling do DB em posição com efeito TELEPORT. Logging para DB errors.
</Aside>