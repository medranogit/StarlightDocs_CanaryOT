---
title: kitchen_knife.lua
description: Wrapper ultra-minimalist que delega para função externa onUseKitchenKnife().
---

import { Card, Aside } from '@astrojs/starlight/components';

## 🛠️ Informações do Arquivo

<Card title="kitchen_knife.lua" icon="pencil">
Handler trivial que age como proxy para onUseKitchenKnife() externo.
</Card>

<Aside type="tip">
**Caminho Original:** `data/scripts/actions/tools/kitchen_knife.lua`

**Linhas:** 8 | **ID do Tool:** 1 (3469) | **Tipo:** Action Handler Wrapper
</Aside>

---

## 📄 Visão Geral do Código

### Resumo Executivo

O script é um wrapper mínimo que delega toda a lógica para função externa onUseKitchenKnife(). Registra 1 kitchen knife tool que quando ativado simplesmente chama função externa passando todos os parâmetros direto. Sem lógica local, sem validações, completamente transparente.

### Estrutura de Funcionalidades

```
HANDLER:
  └─ kitchenKnife.onUse(player, item, fromPosition, target, toPosition, isHotkey)
      └─ return onUseKitchenKnife(player, item, fromPosition, target, toPosition, isHotkey)
          └─ Função externa (não definida neste arquivo)

REGISTRO:
  └─ 1 kitchen knife ID (3469)
```

---

## Variáveis Globais

| Variável | Tipo | Descrição |
|----------|------|-----------|
| kitchenKnife | Action | Objeto handler |

---

## Funções Locais

Não há funções locais neste script.

---

## Constantes e Parâmetros

| Parâmetro | Valor | Descrição |
|-----------|-------|-----------|
| Tool ID | 3469 | Kitchen knife único |

---

## Fluxo de Operação

### Operação Principal

```lua
local kitchenKnife = Action()

function kitchenKnife.onUse(player, item, fromPosition, target, toPosition, isHotkey)
	return onUseKitchenKnife(player, item, fromPosition, target, toPosition, isHotkey)
end

kitchenKnife:id(3469)
kitchenKnife:register()
```

### Execução

```
Jogador usa kitchen knife (3469)
  │
  └─ onUse() dispara
      └─ return onUseKitchenKnife(...)
          └─ Lógica externa
```

---

## Checkpoint de Aprendizagem

### Conceitos-chave

- **Wrapper Pattern**: Handler proxy que delega
- **Parameter Pass-through**: Todos os parâmetros repassados
- **External Function**: onUseKitchenKnife() (definida fora)
- **Single ID**: 1 kitchen knife, 1 handler
- **No Local Logic**: Execução completamente delegada

### Responsabilidades do Handler

1. **Delegar**: Passar para onUseKitchenKnife()
2. **Retornar resultado**: Return direto da função externa

---

## Patterns e Implementação

### Padrão Wrapper/Proxy

Handler que delega:

```lua
function kitchenKnife.onUse(player, item, fromPosition, target, toPosition, isHotkey)
	return onUseKitchenKnife(player, item, fromPosition, target, toPosition, isHotkey)
end
```

### Comparação: Wrappers Simples (Kitchen Knife vs Crowbar vs Stash)

| Aspecto | Kitchen Knife | Crowbar | Stash |
|---------|---|---|---|
| Linhas | 8 | 8 | 8 |
| IDs | 1 | 1 | 1 |
| Delegate | onUseKitchenKnife() | onUseCrowbar() | player:openStash() |
| Tipo | Custom função | Custom função | Player method |
| Padrão | Proxy | Proxy | UI wrapper |

---

## 🤖 Informações da Documentação

<Card title="Documentação: kitchen_knife.lua" icon="star">
Análise de wrapper minimalista que age como proxy para função externa. Demonstra padrão de delegação simples.
</Card>

<Aside type="note">
**Gerado em:** 19 de Fevereiro de 2026 | **Versão:** 1.0 | **Status:** ✅ Documentado

Padrão: Wrapper/proxy delegation | Complexidade: Trivial
</Aside>
