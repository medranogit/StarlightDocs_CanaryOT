---
title: "Spell - heal_party.lua"
description: "Spell de cura para party druid exclusive"
---

import { Aside } from '@astrojs/starlight/components'

## 🛠️ Informações do Arquivo

| Propriedade | Valor |
|---|---|
| **Arquivo** | `heal_party.lua` |
| **Localização** | `data/scripts/spells/party/` |
| **Tipo de Script** | Spell (Instant) |
| **Tipo** | Party Support (Health Regeneration) |
| **Alvo** | Party Members (Area CIRCLE 5x5) |
| **Mana Requerida** | 120 Base (Dinâmico) |
| **Words** | utura mas sio |
| **Spell ID** | 128 |
| **Cooldown** | 2s Individual |
| **GroupCooldown** | 2s |
| **Premium** | Sim |
| **Agressivo** | Não |
| **Self Target** | Sim |
| **Grupo** | support |
| **Vocações** | Druid, Elder Druid |
| **Level** | 32 |
| **Efeito Visual** | CONST_ME_MAGIC_BLUE |
| **Som de Casting** | SOUND_EFFECT_TYPE_SPELL_HEAL_PARTY |
| **Linhas de Código** | 85 |
| **Complexidade** | Intermediária |

**Descrição:**
Spell de cura para membros da party. Aplica health regen +20 HP a cada 2 segundos por 2 minutos. Condition `CONDITION_REGENERATION`. Area `CIRCLE5X5`. Mana dinâmica baseada em quantidade: `0.9^(n-1)*120*n`. Distance check `<= 36`. Validação party + members. Range distance 36 squares. Druid exclusive premium level 32. Words: `utura mas sio`. Spell heal party druid health regen!

---

## 📍 Caminho Original

```
data/scripts/spells/party/heal_party.lua
```

**Tipo Spell:** Instant

**Registração:**
```lua
spell:name("Heal Party")
spell:words("utura mas sio")
spell:group("support")
spell:vocation("druid;true", "elder druid;true")
spell:castSound(SOUND_EFFECT_TYPE_SPELL_HEAL_PARTY)
spell:id(128)
spell:cooldown(2 * 1000)
spell:groupCooldown(2 * 1000)
spell:level(32)
spell:mana(120)
spell:isSelfTarget(true)
spell:isAggressive(false)
spell:isPremium(true)
spell:needLearn(false)
spell:register()
```

---

## 💡 Tip

<Aside type="tip">
**Heal Party - Druid Health Regeneration Support**

Este spell oferece recuperação de health para toda party:

1. **Health Regeneration:**
   - Condition CONDITION_REGENERATION
   - CONDITION_PARAM_HEALTHGAIN: +20 HP
   - CONDITION_PARAM_HEALTHTICKS: 2 segundos (interval)
   - Duração: 2 minutos (120 segundos)
   - Total: 20 * (120/2) = 20 * 60 = 1200 HP!

2. **Mana Dinâmica Escalável:**
   - Base: 120 mana
   - Cálculo: 0.9^(members-1) * 120 * members
   - Exemplo: 2 members = 120 * 2 = 240 total
   - Exemplo: 3 members = 0.9 * 120 * 3 = 324 total
   - Escalagem eficiente!

3. **Area Party CIRCLE 5x5:**
   - Raio: aproximadamente 5-6 squares
   - Só afeta membros dentro da distance
   - Range check `<= 36` squares!

4. **Party Logic Robusta:**
   - Validação: party:getMembers()
   - Inclui leader (membros + 1)
   - Distance check por membro
   - Mínimo 2 afetados!

**Uso Típico:**
```
Druid lvl 32+ (Premium)
// Cast "utura mas sio"
// Sistema obtém party + members
// Valida distance <= 36
// Calcula mana dinâmica
// Aplica +20 HP per 2s
// Duração 2 minutos
// Total: 1200 HP regen!
// Spell heal party health regen!
```

**Diferencial:** Druid exclusive premium party health regen com cooldown rápido 2s e mana dinâmica escalável!
</Aside>

---

## 📄 Visão Geral do Código (Resumo Executivo)

### Propósito Principal

O spell `heal_party` oferece regeneração de health para membros da party com cooldown rápido:

- **Tipo:** Party Support (Health Regeneration)
- **Spell ID:** 128
- **Vocações:** Druid, Elder Druid
- **Level:** 32 (premium!)
- **Efeito:** +20 HP a cada 2s por 2 minutos (1200 total!)
- **Area:** CIRCLE5X5 (raio ~5-6 squares)
- **Mana Base:** 120
- **Mana Dinâmica:** 0.9^(members-1) * 120 * members
- **Distance Check:** `<= 36` squares
- **Cooldown:** 2s (rápido!)
- **Mínimo Afetados:** 2 membros

### Arquitetura

```
local combat = Combat()
├─ setArea(AREA_CIRCLE5X5)
├─ setParameter(COMBAT_PARAM_EFFECT, CONST_ME_MAGIC_BLUE)
├─ setParameter(COMBAT_PARAM_AGGRESSIVE, 0)
└─ execute(creature, var)

local condition = Condition(CONDITION_REGENERATION)
├─ CONDITION_PARAM_SUBID: 1
├─ CONDITION_PARAM_BUFF_SPELL: 1
├─ CONDITION_PARAM_TICKS: 120000 (2 min)
├─ CONDITION_PARAM_HEALTHGAIN: +20
└─ CONDITION_PARAM_HEALTHTICKS: 2000

local baseMana = 120

local spell = Spell("instant")
├─ onCastSpell(creature, var)
│  ├─ Obter party + members
│  ├─ Distance check <= 36
│  ├─ Calcular mana dinâmico
│  ├─ Validar mana suficiente
│  ├─ Execute combat
│  └─ Adicionar condition a cada membro
└─ Configuração spell
```

---

## 🔧 Estrutura de Funcionalidades

### Variável Global: combat

```lua
local combat = Combat()
```

**Propósito:** Instância Combat para área party com efeito.

**Tipo:** Combat

**Parâmetros Configurados:**
- Area: AREA_CIRCLE5X5
- Effect: CONST_ME_MAGIC_BLUE
- Aggressive: 0 (não-agressivo)

---

### Variável Global: condition

```lua
local condition = Condition(CONDITION_REGENERATION)
```

**Propósito:** Condition para regeneração health.

**Tipo:** Condition (CONDITION_REGENERATION)

**Parâmetros Configurados:**
- SUBID: 1
- BUFF_SPELL: 1 (é buff)
- TICKS: 120000 (2 minutos)
- HEALTHGAIN: +20 (HP por tick)
- HEALTHTICKS: 2000 (a cada 2s)

---

### Variável Global: baseMana

```lua
local baseMana = 120
```

**Propósito:** Mana base para cálculo dinâmico.

**Tipo:** Integer

**Valor:** 120

---

### Variável Global: spell

```lua
local spell = Spell("instant")
```

**Propósito:** Instância do spell.

**Tipo:** Instant

---

### Função Global: spell.onCastSpell

```lua
function spell.onCastSpell(creature, var)
	local position = creature:getPosition()
	local party = creature:getParty()
	if not party then
		creature:sendCancelMessage("No party members in range.")
		position:sendMagicEffect(CONST_ME_POFF)
		return false
	end

	local membersList = party:getMembers()
	membersList[#membersList + 1] = party:getLeader()
	if membersList == nil or type(membersList) ~= "table" or #membersList <= 1 then
		creature:sendCancelMessage("No party members in range.")
		position:sendMagicEffect(CONST_ME_POFF)
		return false
	end

	local affectedList = {}
	for _, targetPlayer in ipairs(membersList) do
		if targetPlayer:getPosition():getDistance(position) <= 36 then
			affectedList[#affectedList + 1] = targetPlayer
		end
	end

	local tmp = #affectedList
	if tmp <= 1 then
		creature:sendCancelMessage("No party members in range.")
		position:sendMagicEffect(CONST_ME_POFF)
		return false
	end

	local mana = math.ceil((0.9 ^ (tmp - 1) * baseMana) * tmp)
	if creature:getMana() < mana then
		creature:sendCancelMessage(RETURNVALUE_NOTENOUGHMANA)
		position:sendMagicEffect(CONST_ME_POFF)
		return false
	end

	if not combat:execute(creature, var) then
		creature:sendCancelMessage(RETURNVALUE_NOTPOSSIBLE)
		position:sendMagicEffect(CONST_ME_POFF)
		return false
	end

	creature:addMana(-(mana - baseMana), false)
	creature:addManaSpent((mana - baseMana))

	for _, targetPlayer in ipairs(affectedList) do
		targetPlayer:addCondition(condition)
	end

	return true
end
```

**Propósito:** Cura health de party members.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster (druid) |
| `var` | Variant | Variant (self) |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Cura aplicada |
| `false` | Erro (sem party, sem mana, etc) |

**Lógica Detalhada:**

1. **Validação Party:**
   - getParty() do creature
   - Se não existe: erro

2. **Obtenção Members:**
   - getMembers() da party
   - Adiciona leader: `membersList[#membersList + 1] = party:getLeader()`
   - Validação: nil check, table type, > 1 elementos

3. **Distance Check:**
   - ITera cada member
   - Valida distance com limite de 36 squares
   - Cria affectedList

4. **Mínimo Afetados:**
   - Valida tmp (affected count) > 1
   - Se ≤ 1: erro

5. **Cálculo Mana Dinâmica:**
   - `mana = math.ceil((0.9 ^ (tmp - 1) * baseMana) * tmp)`
   - Escalagem eficiente com decay

6. **Validação Mana:**
   - Verifica creature:getMana() >= mana
   - Se insuficiente: erro

7. **Execute Combat:**
   - Execute area combat
   - Se falha: erro

8. **Mana Gasto:**
   - Subtrai (mana - baseMana) como custo extra
   - Registra gasto

9. **Aplicar Condition:**
   - ITera cada member afetado
   - Adiciona condition REGENERATION +20
   - Duração 2 minutos

---

## 📊 Variáveis Globais

| Nome | Tipo | Escopo | Descrição |
|---|---|---|---|
| `combat` | Combat | Arquivo | Instância Combat area |
| `condition` | Condition | Arquivo | Instância condition regeneration |
| `baseMana` | Integer | Arquivo | Base mana 120 |
| `spell` | Spell | Arquivo | Instância spell |

### Variáveis Locais (onCastSpell)

| Nome | Tipo | Descrição |
|---|---|---|
| `position` | Position | Posição do caster |
| `party` | Party | Party do caster |
| `membersList` | Table | Membros + leader |
| `affectedList` | Table | Membros dentro de range |
| `tmp` | Integer | Quantidade afetada |
| `mana` | Integer | Mana total requerida |
| `targetPlayer` | Player | Iteração members |

---

## 🎯 Padrões Identificados

### Padrão 1: Party Validation Completa

```lua
local party = creature:getParty()
if not party then
	-- erro
end

local membersList = party:getMembers()
membersList[#membersList + 1] = party:getLeader()
if membersList == nil or type(membersList) ~= "table" or #membersList <= 1 then
	-- erro
end
```

**Benefício:** Validação completa existência + tipo + quantidade com fallback leader!

### Padrão 2: Distance Filtering Loop

```lua
for _, targetPlayer in ipairs(membersList) do
	if targetPlayer:getPosition():getDistance(position) <= 36 then
		affectedList[#affectedList + 1] = targetPlayer
	end
end
```

**Benefício:** Filtra por distance máximo 36 squares!

### Padrão 3: Mana Dinâmica com Math.ceil

```lua
local mana = math.ceil((0.9 ^ (tmp - 1) * baseMana) * tmp)
```

**Benefício:** Calcula mana total com decay eficiente!

### Padrão 4: Condition Pre-Criada Reutilizada

```lua
for _, targetPlayer in ipairs(affectedList) do
	targetPlayer:addCondition(condition)
end
```

**Benefício:** Condition única aplicada a todos os membros!

---

## 🤖 Documentação das Funções

### spell.onCastSpell(creature, var)

**Descrição:** Cura health (+20 a cada 2s por 2min) de party members em range.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster (druid lvl 32+) |
| `var` | Variant | Variant (self) |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Cura aplicada a party |
| `false` | Erro (sem party/mana/range) |

**Requisitos:**
- Ter party (mínimo 2 membros)
- Members dentro de 36 squares
- Mana suficiente (dinâmica)
- Druid ou Elder Druid
- Level 32+
- Premium account

**Health Regeneration Details:**

| Feature | Value |
|---|---|
| HP por Tick | +20 |
| Interval | 2 segundos |
| Duração Total | 2 minutos (120s) |
| Ticks Totais | 60 (2min / 2s) |
| HP Total Regen | 1200 HP! |

**Exemplo de Uso:**
```lua
-- Druid lvl 32+, in party com 3 membros
-- Cast "utura mas sio"
-- Sistema obtém party + members
-- Filtra por distance <= 36
-- Calcula mana: ceil(0.9 * 120 * 3) = 324
// Se tem mana: apply condition +20 HP per 2s
// Duração 2 minutos para cada membro
// Total: 1200 HP regenerado!
// Efeito CONST_ME_MAGIC_BLUE
// Cooldown 2s (rápido!)
// Spell heal party health regen!
```

---

## 📌 Resumo

**Heal Party** é um spell druid exclusive premium que oferece regeneração de health para membros da party. Com condition CONDITION_REGENERATION (+20 HP a cada 2 segundos por 2 minutos = 1200 total!), área CIRCLE5X5, distance check 36 squares, mana dinâmica 0.9^(n-1)*120*n escalável, validação party completa, mínimo 2 afetados, spell ID 128, palabras "utura mas sio", som SOUND_EFFECT_TYPE_SPELL_HEAL_PARTY, level 32, cooldown 2s (rápido!), groupCooldown 2s, isSelfTarget true, isAggressive false, isPremium true. Spell heal party druid health regeneration support!
