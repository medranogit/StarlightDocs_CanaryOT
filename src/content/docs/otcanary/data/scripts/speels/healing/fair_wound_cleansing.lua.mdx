---
title: "Spell - fair_wound_cleansing.lua"
description: "Spell de cura premium ultra high com dispel paralyze para knights level 300"
---

import { Aside } from '@astrojs/starlight/components'

## 🛠️ Informações do Arquivo

| Propriedade | Valor |
|---|---|
| **Arquivo** | `fair_wound_cleansing.lua` |
| **Localização** | `data/scripts/spells/healing/` |
| **Tipo de Script** | Spell (Instant) |
| **Tipo** | Healing/Dispel Combat |
| **Vocações** | Knight, Elite Knight |
| **Level Requerido** | 300 |
| **Mana Requerida** | 90 |
| **Soul Requerida** | Nenhuma |
| **Words** | exura med ico |
| **Grupo** | healing |
| **Spell ID** | 239 |
| **Cooldown** | 1s |
| **GroupCooldown** | 1s |
| **Premium** | Sim |
| **Agressivo** | Não |
| **Self Target** | Sim |
| **Tipo Combat** | Healing + Dispel CONDITION_PARALYZE |
| **Efeito Visual** | CONST_ME_MAGIC_BLUE |
| **Som de Casting** | SOUND_EFFECT_TYPE_SPELL_FAIR_WOUND_CLEANSING |
| **Linhas de Código** | 33 |
| **Complexidade** | Intermediária |

**Descrição:**
Spell ultra high-level premium de cura suprema + dispel paralyze para knights nível 300 (END-GAME!!). Fórmula healing com multiplicador *2! Remove CONDITION_PARALYZE. 90 mana (moderado!). Cooldown 1s (rápido!). Spell ID 239. Som especial fair wound cleansing. Knight exclusive premium. Spell de healing ULTRA HIGH+DISPEL end-game knight!

---

## 📍 Caminho Original

```
data/scripts/spells/healing/fair_wound_cleansing.lua
```

**Tipo Spell:** Instant

**Registração:**
```lua
spell:group("healing")
spell:id(239)
spell:name("Fair Wound Cleansing")
spell:words("exura med ico")
spell:castSound(SOUND_EFFECT_TYPE_SPELL_FAIR_WOUND_CLEANSING)
spell:level(300)
spell:mana(90)
spell:isPremium(true)
spell:isSelfTarget(true)
spell:cooldown(1000)
spell:groupCooldown(1000)
spell:isAggressive(false)
spell:vocation("knight;true", "elite knight;true")
spell:needLearn(false)
spell:register()
```

---

## 💡 Tip

<Aside type="tip">
**Cura Fair Wound Cleansing - Ultra High-Level 300 Knight Premium**

Este spell implementa um sistema healing supremo + dispel paralyze:

1. **Cura Suprema Ultra High:**
   - Level 300 (END-GAME!!)
   - 90 mana (moderado!)
   - Fórmula com MULTIPLICADOR *2: ((level * 0.2 + magicLevel * 4 + 25) até (level * 0.2 + magicLevel * 7.95 + 51)) * 2
   - Cura ultra high knight premium base!

2. **Dispel Paralyze:**
   - Remove CONDITION_PARALYZE
   - 1s cooldown (rápido!)
   - Self target (cura + dispel self)
   - Combat healing system!

3. **Knight Exclusive Premium Ultra:**
   - Knight + Elite Knight (2 vocações)
   - Grupo healing
   - Não agressivo
   - Spell cura healing ultra high knight exclusive!

4. **Tipo Combat:**
   - Combat system com callback
   - Efeito visual: CONST_ME_MAGIC_BLUE
   - Som especial fair wound cleansing
   - Suprema healing + paralyze dispel combo!

**Uso Típico:**
```
Knight lvl 300+ (Premium)
// Fair Wound Cleansing = ultra high healing supremo
// Remove paralyze e cura healing massivo
// 90 mana = moderado! (surpreendente para ultra!)
// 1s cooldown = rápido!
// Healing skillpoint knight exclusive premium ultra
```

**Diferencial:** Cura ultra high knight com multiplicador *2 + paralyze dispel!
</Aside>

---

## 📄 Visão Geral do Código (Resumo Executivo)

### Propósito Principal

O spell `fair_wound_cleansing` oferece cura suprema + dispel paralyze:

- **Tipo:** Healing/Dispel Combat
- **Spell ID:** 239
- **Dispel:** CONDITION_PARALYZE
- **Mana:** 90 (moderado!)
- **Level:** 300 (END-GAME!)
- **Fórmula Healing:** ((level * 0.2 + magicLevel * 4 + 25) até (level * 0.2 + magicLevel * 7.95 + 51)) * 2
- **Cooldown:** 1s (rápido!)
- **Tipo Combat:** Healing + Dispel

### Arquitetura

```
local combat = Combat()
├─ setParameter(COMBAT_PARAM_TYPE, COMBAT_HEALING)
├─ setParameter(COMBAT_PARAM_EFFECT, CONST_ME_MAGIC_BLUE)
├─ setParameter(COMBAT_PARAM_DISPEL, CONDITION_PARALYZE)
├─ setParameter(COMBAT_PARAM_AGGRESSIVE, false)
└─ setCallback(CALLBACK_PARAM_LEVELMAGICVALUE, "onGetFormulaValues")

local spell = Spell("instant")
├─ onCastSpell(creature, variant)
│  └─ return combat:execute(creature, variant)
└─ Configuração spell
   ├─ group: healing
   ├─ mana: 90
   ├─ level: 300
   └─ vocation: knight exclusive
```

---

## 🔧 Estrutura de Funcionalidades

### Variável Global: combat

```lua
local combat = Combat()
```

**Propósito:** Instância do sistema Combat para healing + dispel.

**Tipo:** Combat

**Parâmetros Configurados:**
- COMBAT_PARAM_TYPE: COMBAT_HEALING
- COMBAT_PARAM_EFFECT: CONST_ME_MAGIC_BLUE
- COMBAT_PARAM_DISPEL: CONDITION_PARALYZE
- COMBAT_PARAM_AGGRESSIVE: false

---

### Variável Global: spell

```lua
local spell = Spell("instant")
```

**Propósito:** Instância do spell de cura.

**Tipo:** Instant (sem cast time)

---

### Função Global: onGetFormulaValues

```lua
function onGetFormulaValues(_player, level, magicLevel)
	local min = (level * 0.2 + magicLevel * 4 + 25) * 2
	local max = (level * 0.2 + magicLevel * 7.95 + 51) * 2
	return min, max
end
```

**Propósito:** Calcula valores de healing supremos com multiplicador *2 baseado em level + magic level.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `_player` | Player | Caster (não utilizado) |
| `level` | Integer | Level do player |
| `magicLevel` | Integer | Magic level do player |

**Retorno:**

| Valor | Descrição |
|---|---|
| `min` | Healing mínimo |
| `max` | Healing máximo |

**Fórmula de Healing Suprema com Multiplicador *2:**
```
min = (level * 0.2 + magicLevel * 4 + 25) * 2
max = (level * 0.2 + magicLevel * 7.95 + 51) * 2
Range = [min, max]
```

---

### Função Global: spell.onCastSpell

```lua
function spell.onCastSpell(creature, variant)
	return combat:execute(creature, variant)
end
```

**Propósito:** Executa sistema Combat healing supremo + dispel.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster |
| `variant` | Variant | Target variant |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Healing + dispel executado |
| `false` | Falha execução |

---

## 📊 Variáveis Globais

| Nome | Tipo | Escopo | Descrição |
|---|---|---|---|
| `combat` | Combat | Arquivo | Instância Combat healing |
| `spell` | Spell | Arquivo | Instância spell |

---

## 🎯 Padrões Identificados

### Padrão 1: Combat Healing Supremo + Dispel Ultra

```lua
local combat = Combat()
combat:setParameter(COMBAT_PARAM_TYPE, COMBAT_HEALING)
combat:setParameter(COMBAT_PARAM_DISPEL, CONDITION_PARALYZE)
```

**Benefício:** Combine healing supremo + dispel em um único combat action!

### Padrão 2: Knight Exclusive Ultra High-Level

```lua
spell:vocation("knight;true", "elite knight;true")
spell:level(300)
spell:isPremium(true)
spell:group("healing")
```

**Benefício:** Knight exclusive premium ultra high-level (300 - END-GAME!)!

### Padrões 3: Custos Moderados Ultra High

```lua
spell:mana(90) -- Surpreendentemente moderado!
spell:cooldown(1000)
spell:groupCooldown(1000)
```

**Benefício:** Custos moderados (mana 90) para ultra high-level!

### Padrão 4: Callback Fórmula Suprema com Multiplicador

```lua
combat:setCallback(CALLBACK_PARAM_LEVELMAGICVALUE, "onGetFormulaValues")
```

**Benefício:** Healing dinâmico supremo com multiplicador *2 baseado em level + magic level!

---

## 🤖 Documentação das Funções

### onGetFormulaValues(_player, level, magicLevel)

**Descrição:** Calcula healing cure supremo com multiplicador *2 baseado em level + magic level.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `_player` | Player | Caster (não utilizado) |
| `level` | Integer | Level do player |
| `magicLevel` | Integer | Magic level do player |

**Retorno:**

| Valor | Descrição |
|---|---|
| `min` | Healing mínimo: (level * 0.2 + magicLevel * 4 + 25) * 2 |
| `max` | Healing máximo: (level * 0.2 + magicLevel * 7.95 + 51) * 2 |

**Exemplo de Cálculo:**
```lua
-- Knight lvl 300, magic lvl 0
-- min = (300 * 0.2 + 0 * 4 + 25) * 2 = (60 + 0 + 25) * 2 = 85 * 2 = 170
-- max = (300 * 0.2 + 0 * 7.95 + 51) * 2 = (60 + 0 + 51) * 2 = 111 * 2 = 222

-- Knight lvl 300, magic lvl 50
-- min = (300 * 0.2 + 50 * 4 + 25) * 2 = (60 + 200 + 25) * 2 = 285 * 2 = 570
-- max = (300 * 0.2 + 50 * 7.95 + 51) * 2 = (60 + 397.5 + 51) * 2 = 508.5 * 2 = 1017
```

### spell.onCastSpell(creature, variant)

**Descrição:** Executa healing supremo + paralyze dispel para o caster.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Knight ou Elite Knight |
| `variant` | Variant | Target variant |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Healing + dispel executado |
| `false` | Falha execução |

**Requisitos:**
- Level 300+
- 90 mana
- Premium account

**Exemplo de Uso:**
```lua
// Ao lançar "exura med ico" (Premium)
-- combat:execute(creature, variant)
-- Cura healing supremo value baseado em fórmula com multiplicador *2
-- Remove CONDITION_PARALYZE
-- Displays CONST_ME_MAGIC_BLUE effect
-- Cooldown 1s aplicado (rápido!)
```

---

## 📌 Resumo

**Fair Wound Cleansing** é um spell de cura ultra high-level knight exclusive premium que oferece healing supremo + remoção de paralyze. Com 90 mana (moderado!) e level 300 (END-GAME!), implementa fórmula dinâmica healing suprema com multiplicador *2: ((level * 0.2 + magicLevel * 4 + 25) até (level * 0.2 + magicLevel * 7.95 + 51)) * 2. Cooldown rápido 1s, self target, grupo healing, som especial SOUND_EFFECT_TYPE_SPELL_FAIR_WOUND_CLEANSING. Usa Combat system com dispel CONDITION_PARALYZE - spell supremo end-game para knights em healing ultra high-level com paralyze removal!
