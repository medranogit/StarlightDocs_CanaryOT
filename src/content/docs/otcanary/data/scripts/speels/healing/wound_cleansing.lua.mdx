---
title: "Spell - wound_cleansing.lua"
description: "Spell de cicatrização knight exclusive early-level 8"
---

import { Aside } from '@astrojs/starlight/components'

## 🛠️ Informações do Arquivo

| Propriedade | Valor |
|---|---|
| **Arquivo** | `wound_cleansing.lua` |
| **Localização** | `data/scripts/spells/healing/` |
| **Tipo de Script** | Spell (Instant) |
| **Tipo** | Healing Combat (Self) |
| **Vocações** | Knight, Elite Knight |
| **Level Requerido** | 8 |
| **Mana Requerida** | 40 |
| **Soul Requerida** | Nenhuma |
| **Words** | exura ico |
| **Grupo** | healing |
| **Spell ID** | 123 |
| **Cooldown** | 1s |
| **GroupCooldown** | 1s |
| **Premium** | Não |
| **Agressivo** | Não |
| **Self Target** | Sim |
| **Tipo Combat** | Healing LOW + Dispel CONDITION_PARALYZE |
| **Efeito Visual** | CONST_ME_MAGIC_BLUE |
| **Som de Casting** | SOUND_EFFECT_TYPE_SPELL_WOUND_CLEANSING |
| **Linhas de Código** | 31 |
| **Complexidade** | Simples |

**Descrição:**
Spell de cicatrização knight exclusive early-level 8. Sistema Combat com fórmula healing dinâmica LOW. Remove CONDITION_PARALYZE. Mana: 40 (moderado!). Cooldown: 1s (rápido!). Spell ID 123. Self target (isSelfTarget true). Free (não premium). needLearn false. 2 vocações: Knight, Elite Knight. Fórmula: (level * 0.2 + magicLevel * 4) + 25 até (level * 0.2 + magicLevel * 7.95) + 51. Validação oficial. Spell cicatrização knight exclusive EARLY-LEVEL!

---

## 📍 Caminho Original

```
data/scripts/spells/healing/wound_cleansing.lua
```

**Tipo Spell:** Instant

**Registração:**
```lua
spell:name("Wound Cleansing")
spell:words("exura ico")
spell:group("healing")
spell:vocation("knight;true", "elite knight;true")
spell:castSound(SOUND_EFFECT_TYPE_SPELL_WOUND_CLEANSING)
spell:id(123)
spell:cooldown(1 * 1000)
spell:groupCooldown(1 * 1000)
spell:level(8)
spell:mana(40)
spell:isSelfTarget(true)
spell:isAggressive(false)
spell:register()
```

---

## 💡 Tip

<Aside type="tip">
**Cicatrização - Early-Level 8 Knight Exclusive Low Healing**

Este spell implementa um sistema de cicatrização com fórmula dinâmica baixa:

1. **Cicatrização Early-Level:**
   - Level 8 (EARLY!)
   - 40 mana (moderado!)
   - Fórmula healing: (level * 0.2 + magicLevel * 4) + 25 até (level * 0.2 + magicLevel * 7.95) + 51
   - Cicatrização early-level knight!

2. **Knight Exclusive Free:**
   - 2 vocações: Knight + Elite Knight
   - Spell ID 123
   - Free (não premium!)
   - Group healing
   - Knight exclusive!

3. **Cooldown Rápido:**
   - Cooldown: 1s (RÁPIDO!)
   - GroupCooldown: 1s
   - Rapid casting!

4. **Tipo Combat:**
   - Combat system com callback
   - Efeito visual: CONST_ME_MAGIC_BLUE
   - Som especial wound cleansing
   - Dispel CONDITION_PARALYZE!

**Uso Típico:**
```
Knight/Elite Knight lvl 8+ (Free!)
// Wound Cleansing = early-level cicatrização
// Cura LOW: (level * 0.2 + magicLevel * 4) + 25 até +51
// Remove paralyze e cura healing self
// 40 mana = moderado!
// Cooldown 1s = rápido!
// Spell cicatrização knight exclusive early!
```

**Diferencial:** Cicatrização early-level knight exclusive FREE com fórmula validada oficial!
</Aside>

---

## 📄 Visão Geral do Código (Resumo Executivo)

### Propósito Principal

O spell `wound_cleansing` oferece cicatrização low para knights com fórmula dinâmica:

- **Tipo:** Healing Combat (Self)
- **Spell ID:** 123
- **Dispel:** CONDITION_PARALYZE
- **Mana:** 40 (moderado!)
- **Level:** 8 (early!)
- **Fórmula Healing:** (level * 0.2 + magicLevel * 4) + 25 até (level * 0.2 + magicLevel * 7.95) + 51
- **Cooldown:** 1s (rápido!)
- **Vocações:** 2 (knight exclusive!)

### Arquitetura

```
local combat = Combat()
├─ setParameter(COMBAT_PARAM_TYPE, COMBAT_HEALING)
├─ setParameter(COMBAT_PARAM_EFFECT, CONST_ME_MAGIC_BLUE)
├─ setParameter(COMBAT_PARAM_DISPEL, CONDITION_PARALYZE)
├─ setParameter(COMBAT_PARAM_AGGRESSIVE, false)
└─ setCallback(CALLBACK_PARAM_LEVELMAGICVALUE, "onGetFormulaValues")

local spell = Spell("instant")
├─ onCastSpell(creature, variant)
│  └─ return combat:execute(creature, variant)
└─ Configuração spell
   ├─ group: healing
   ├─ mana: 40
   ├─ isSelfTarget: true
   └─ vocation: 2 classes
```

---

## 🔧 Estrutura de Funcionalidades

### Variável Global: combat

```lua
local combat = Combat()
```

**Propósito:** Instância do sistema Combat para cicatrização + dispel.

**Tipo:** Combat

**Parâmetros Configurados:**
- COMBAT_PARAM_TYPE: COMBAT_HEALING
- COMBAT_PARAM_EFFECT: CONST_ME_MAGIC_BLUE
- COMBAT_PARAM_DISPEL: CONDITION_PARALYZE
- COMBAT_PARAM_AGGRESSIVE: false

---

### Variável Global: spell

```lua
local spell = Spell("instant")
```

**Propósito:** Instância do spell de cicatrização.

**Tipo:** Instant (sem cast time)

---

### Função Global: onGetFormulaValues

```lua
function onGetFormulaValues(player, level, magicLevel)
	local min = (level * 0.2 + magicLevel * 4) + 25
	local max = (level * 0.2 + magicLevel * 7.95) + 51
	return min, max
end
```

**Propósito:** Calcula valores low de healing baseado em level + magic level.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `player` | Player | Caster |
| `level` | Integer | Level do player |
| `magicLevel` | Integer | Magic level do player |

**Retorno:**

| Valor | Descrição |
|---|---|
| `min` | Healing mínimo low |
| `max` | Healing máximo low |

**Fórmula de Cura:**
```
min = level * 0.2 + magicLevel * 4 + 25
max = level * 0.2 + magicLevel * 7.95 + 51
Range = [min, max]
```

**Validação Oficial:**
```
// Comparado com official tibia
// Compared date: 08/03/21(m/d/y)
// Fórmula validada!
```

**Exemplo de Cálculo:**
```lua
-- Knight lvl 8, magic lvl 0
-- min = 8 * 0.2 + 0 * 4 + 25 = 1.6 + 0 + 25 = 26.6 ≈ 26
-- max = 8 * 0.2 + 0 * 7.95 + 51 = 1.6 + 0 + 51 = 52.6 ≈ 52

-- Knight lvl 30, magic lvl 10
-- min = 30 * 0.2 + 10 * 4 + 25 = 6 + 40 + 25 = 71
-- max = 30 * 0.2 + 10 * 7.95 + 51 = 6 + 79.5 + 51 = 136.5 ≈ 136
```

---

### Função Global: spell.onCastSpell

```lua
function spell.onCastSpell(creature, variant)
	return combat:execute(creature, variant)
end
```

**Propósito:** Executa sistema Combat cicatrização + dispel.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster |
| `variant` | Variant | Variant (self) |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Healing + dispel executado |
| `false` | Falha execução |

---

## 📊 Variáveis Globais

| Nome | Tipo | Escopo | Descrição |
|---|---|---|---|
| `combat` | Combat | Arquivo | Instância Combat healing |
| `spell` | Spell | Arquivo | Instância spell |

---

## 🎯 Padrões Identificados

### Padrão 1: Combat Healing Low + Dispel Knight

```lua
local combat = Combat()
combat:setParameter(COMBAT_PARAM_TYPE, COMBAT_HEALING)
combat:setParameter(COMBAT_PARAM_DISPEL, CONDITION_PARALYZE)
```

**Benefício:** Cicatrização + dispel em um único combat action!

### Padrão 2: Knight Exclusive Early-Level Free

```lua
spell:vocation("knight;true", "elite knight;true")
spell:level(8)
spell:mana(40)
spell:needLearn(false)
```

**Benefício:** Free early-level knight exclusive!

### Padrão 3: Callback Fórmula Dinâmica Low

```lua
combat:setCallback(CALLBACK_PARAM_LEVELMAGICVALUE, "onGetFormulaValues")
-- Scaling: magicLevel * 4/7.95 (BAIXO!)
```

**Benefício:** Healing dinâmico low baseado em magic level!

### Padrão 4: Cooldown Rápido 1s

```lua
spell:cooldown(1 * 1000)
spell:groupCooldown(1 * 1000)
```

**Benefício:** Cooldown rápido para cast frequente!

---

## 🤖 Documentação das Funções

### onGetFormulaValues(player, level, magicLevel)

**Descrição:** Calcula cicatrização low baseado em level + magic level.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `player` | Player | Caster |
| `level` | Integer | Level do player |
| `magicLevel` | Integer | Magic level do player |

**Retorno:**

| Valor | Descrição |
|---|---|
| `min` | Healing mínimo low: level * 0.2 + magicLevel * 4 + 25 |
| `max` | Healing máximo low: level * 0.2 + magicLevel * 7.95 + 51 |

### spell.onCastSpell(creature, variant)

**Descrição:** Executa cicatrização low + paralyze dispel.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster (knight) |
| `variant` | Variant | Variant (self) |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Healing + dispel executado |
| `false` | Falha execução |

**Requisitos:**
- Level 8+
- 40 mana (moderado!)
- Vocação: Knight ou Elite Knight
- Self target
- Free (não premium!)

**Exemplo de Uso:**
```lua
// Ao lançar "exura ico" (Level 8+)
-- combat:execute(creature, variant)
-- Cura healing low baseado em fórmula
-- Remove CONDITION_PARALYZE
-- Displays CONST_ME_MAGIC_BLUE effect
-- Cooldown 1s aplicado
-- Vocações: Knight, Elite Knight
```

---

## 📌 Resumo

**Wound Cleansing** é um spell de cicatrização early-level knight exclusive que oferece healing low + remoção de paralyze. Com fórmula dinâmica healing (level * 0.2 + magicLevel * 4) + 25 até (level * 0.2 + magicLevel * 7.95) + 51, 40 mana (moderado!), cooldown 1s (rápido!), spell ID 123, grupo healing, efeito visual CONST_ME_MAGIC_BLUE, sons especiais SOUND_EFFECT_TYPE_SPELL_WOUND_CLEANSING. Self target, FREE (não premium!), needLearn false, 2 vocações (Knight, Elite Knight). Spell early-level knight exclusive FREE cicatrização low!
