---
title: "Spell - nature's_embrace.lua"
description: "Spell de abraço natural target premium level 300 druid END-GAME"
---

import { Aside } from '@astrojs/starlight/components'

## 🛠️ Informações do Arquivo

| Propriedade | Valor |
|---|---|
| **Arquivo** | `nature's_embrace.lua` |
| **Localização** | `data/scripts/spells/healing/` |
| **Tipo de Script** | Spell (Instant - Target) |
| **Tipo** | Healing Combat (Target Friend) |
| **Vocações** | Druid, Elder Druid |
| **Level Requerido** | 300 |
| **Mana Requerida** | 400 |
| **Soul Requerida** | Nenhuma |
| **Words** | exura gran sio |
| **Grupo** | healing |
| **Spell ID** | 242 |
| **Cooldown** | 60s |
| **GroupCooldown** | 1s |
| **Premium** | Sim |
| **Agressivo** | Não |
| **Alvo Requerido** | Sim |
| **Player Name Param** | Sim |
| **Allow Self** | Não (validação em onCastSpell!) |
| **Block Walls** | Sim |
| **Tipo Combat** | Healing ULTRA + Dispel CONDITION_PARALYZE |
| **Efeito Visual** | CONST_ME_MAGIC_BLUE |
| **Som de Casting** | SOUND_EFFECT_TYPE_SPELL_NATURES_EMBRACE |
| **Linhas de Código** | 39 |
| **Complexidade** | Intermediária-Alta |

**Descrição:**
Spell de abraço natural premium target level 300 druid exclusive END-GAME. Sistema Combat com fórmula healing dinâmica ULTRA. Remove CONDITION_PARALYZE. Mana: 400 (GIGANTE!). Cooldown: 60s (longo!). Spell ID 242. Target friend específico com validação de self-cast (rejeita!). Premium exclusive. 2 vocações: Druid, Elder Druid. Validação: if creature:isPlayer() and var:getNumber() == creature:getId() then reject POFF. Blocking Walls ativado. Spell de abraço natural target friend druid PREMIUM END-GAME ULTRA!

---

## 📍 Caminho Original

```
data/scripts/spells/healing/nature's_embrace.lua
```

**Tipo Spell:** Instant (Target)

**Registração:**
```lua
spell:group("healing")
spell:id(242)
spell:name("Nature's Embrace")
spell:words("exura gran sio")
spell:castSound(SOUND_EFFECT_TYPE_SPELL_NATURES_EMBRACE)
spell:level(300)
spell:mana(400)
spell:isPremium(true)
spell:needTarget(true)
spell:cooldown(60 * 1000)
spell:groupCooldown(1 * 1000)
spell:isAggressive(false)
spell:isBlockingWalls(true)
spell:hasParams(true)
spell:hasPlayerNameParam(true)
spell:vocation("druid;true", "elder druid;true")
spell:needLearn(false)
spell:register()
```

---

## 💡 Tip

<Aside type="tip">
**Abraço Natural - Premium Level 300 Druid Target END-GAME**

Este spell implementa um sistema de cura target ultra com fórmula dinâmica extrema:

1. **Abraço Natural Premium END-GAME:**
   - Level 300 (END-GAME ULTRA!)
   - 400 mana (GIGANTE!)
   - Fórmula healing: (level / 2.5) + (maglevel * 20) até (level / 2.5) + (maglevel * 28)
   - Abraço natural ultra healing END-GAME druid!

2. **Druid Exclusive Premium Target:**
   - 2 vocações: Druid + Elder Druid
   - Spell ID 242
   - Premium exclusive
   - Target friend específico (hasPlayerNameParam!)
   - Druid exclusive ultimate target!

3. **Validação Auto-Cast:**
   - needTarget: true
   - hasPlayerNameParam: true
   - Validação: rejeita self-cast (var:getNumber() == creature:getId())
   - CONST_ME_POFF efeito rejection!
   - Smart self-prevention!

4. **Tipo Combat:**
   - Combat system com callback
   - Efeito visual: CONST_ME_MAGIC_BLUE
   - Som especial nature's embrace
   - Dispel CONDITION_PARALYZE!
   - Blocking Walls: true!

**Uso Típico:**
```
Druid/Elder Druid lvl 300+ (Premium)
// Nature's Embrace = END-GAME cura target
// Formula: (level / 2.5) + (maglevel * 20) até +28
// Rejeita self-cast (POFF!)
// Remove paralyze e cura ultra healing target
// 400 mana = investimento gigante!
// Cooldown 60s = spell estratégico
// Spell abraço natural premium END-GAME!
```

**Diferencial:** Cura target ultra END-GAME druid premium com auto-reject self-cast + formula TODO note!
</Aside>

---

## 📄 Visão Geral do Código (Resumo Executivo)

### Propósito Principal

O spell `nature's_embrace` oferece healing target ultra para amigos com validação de self-cast e fórmula dinâmica extrema:

- **Tipo:** Healing Combat (Target Friend)
- **Spell ID:** 242
- **Dispel:** CONDITION_PARALYZE
- **Mana:** 400 (gigante!)
- **Level:** 300 (end-game!)
- **Fórmula Healing:** (level / 2.5) + (maglevel * 20) até (level / 2.5) + (maglevel * 28)
- **Cooldown:** 60s (estratégico!)
- **Vocações:** 2 (druid exclusive!)

### Arquitetura

```
local combat = Combat()
├─ setParameter(COMBAT_PARAM_TYPE, COMBAT_HEALING)
├─ setParameter(COMBAT_PARAM_EFFECT, CONST_ME_MAGIC_BLUE)
├─ setParameter(COMBAT_PARAM_AGGRESSIVE, 0)
├─ setParameter(COMBAT_PARAM_DISPEL, CONDITION_PARALYZE)
└─ setCallback(CALLBACK_PARAM_LEVELMAGICVALUE, "onGetFormulaValues")

local spell = Spell("instant")
├─ onCastSpell(creature, var)
│  ├─ Validação: var:getNumber() == creature:getId()
│  ├─ Se self-cast: CONST_ME_POFF + reject
│  └─ Senão: combat:execute()
└─ Configuração spell
   ├─ group: healing
   ├─ mana: 400
   ├─ needTarget: true
   ├─ hasPlayerNameParam: true
   └─ vocation: 2 classes
```

---

## 🔧 Estrutura de Funcionalidades

### Variável Global: combat

```lua
local combat = Combat()
```

**Propósito:** Instância do sistema Combat para healing target ultra + dispel.

**Tipo:** Combat

**Parâmetros Configurados:**
- COMBAT_PARAM_TYPE: COMBAT_HEALING
- COMBAT_PARAM_EFFECT: CONST_ME_MAGIC_BLUE
- COMBAT_PARAM_AGGRESSIVE: 0
- COMBAT_PARAM_DISPEL: CONDITION_PARALYZE

---

### Variável Global: spell

```lua
local spell = Spell("instant")
```

**Propósito:** Instância do spell de abraço.

**Tipo:** Instant (sem cast time)

---

### Função Global: onGetFormulaValues

```lua
function onGetFormulaValues(player, level, maglevel)
	local min = (level / 2.5) + (maglevel * 20)
	local max = (level / 2.5) + (maglevel * 28)
	return min, max
end
```

**Propósito:** Calcula valores ULTRA de healing baseado em level + magic level.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `player` | Player | Caster |
| `level` | Integer | Level do player |
| `maglevel` | Integer | Magic level do player |

**Retorno:**

| Valor | Descrição |
|---|---|
| `min` | Healing mínimo ULTRA |
| `max` | Healing máximo ULTRA |

**Fórmula de Cura:**
```
min = level / 2.5 + maglevel * 20
max = level / 2.5 + maglevel * 28
Range = [min, max]
```

**Validação Oficial:**
```
// TibiaWiki says x2 but need more accuracy
// Formula: TODO - Formulas need accuracy validation
// Nota de desenvolvimento!
```

**Exemplo de Cálculo:**
```lua
-- Druid lvl 300, magic lvl 100
-- min = 300 / 2.5 + 100 * 20 = 120 + 2000 = 2120
-- max = 300 / 2.5 + 100 * 28 = 120 + 2800 = 2920

-- Druid lvl 300, magic lvl 150
-- min = 300 / 2.5 + 150 * 20 = 120 + 3000 = 3120
-- max = 300 / 2.5 + 150 * 28 = 120 + 4200 = 4320
```

---

### Função Global: spell.onCastSpell

```lua
function spell.onCastSpell(creature, var)
	if creature:isPlayer() and var:getNumber() == creature:getId() then
		creature:sendCancelMessage("You can't cast this spell to yourself.")
		creature:getPosition():sendMagicEffect(CONST_ME_POFF)
		return false
	else
		return combat:execute(creature, var)
	end
end
```

**Propósito:** Executa healing target ultra com validação de self-cast.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster |
| `var` | Variant | Target variant |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Healing target executado |
| `false` | Self-cast rejeitado |

---

## 📊 Variáveis Globais

| Nome | Tipo | Escopo | Descrição |
|---|---|---|---|
| `combat` | Combat | Arquivo | Instância Combat healing |
| `spell` | Spell | Arquivo | Instância spell |

---

## 🎯 Padrões Identificados

### Padrão 1: Validação Smart Self-Cast Rejection

```lua
if creature:isPlayer() and var:getNumber() == creature:getId() then
	creature:sendCancelMessage("You can't cast this spell to yourself.")
	creature:getPosition():sendMagicEffect(CONST_ME_POFF)
	return false
end
```

**Benefício:** Previne auto-casting com mensagem + efeito visual!

### Padrão 2: Druid Exclusive Premium Target END-GAME

```lua
spell:vocation("druid;true", "elder druid;true")
spell:level(300)
spell:isPremium(true)
spell:mana(400)
spell:needTarget(true)
spell:hasPlayerNameParam(true)
```

**Benefício:** Premium END-GAME target healing druid exclusive!

### Padrão 3: Callback Fórmula Dinâmica ULTRA

```lua
combat:setCallback(CALLBACK_PARAM_LEVELMAGICVALUE, "onGetFormulaValues")
-- Scaling: maglevel * 20/28 (MUITO ALTO!)
-- level / 2.5 (nível contribui!)
```

**Benefício:** Healing dinâmico ULTRA baseado em level + magic level!

### Padrão 4: Cooldown Estratégico Longo

```lua
spell:cooldown(60 * 1000) -- 60 segundos!
spell:groupCooldown(1 * 1000)
```

**Benefício:** Cooldown longo balanceado com agrupamento rápido!

---

## 🤖 Documentação das Funções

### onGetFormulaValues(player, level, maglevel)

**Descrição:** Calcula healing target ULTRA baseado em level + magic level.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `player` | Player | Caster |
| `level` | Integer | Level do player |
| `maglevel` | Integer | Magic level do player |

**Retorno:**

| Valor | Descrição |
|---|---|
| `min` | Healing mínimo ULTRA: level / 2.5 + maglevel * 20 |
| `max` | Healing máximo ULTRA: level / 2.5 + maglevel * 28 |

### spell.onCastSpell(creature, var)

**Descrição:** Executa healing target ULTRA com validação de self-cast anti-cheat.

**Parâmetros:**

| Nome | Tipo | Descrição |
|---|---|---|
| `creature` | Creature | Caster (druid/elder druid) |
| `var` | Variant | Target variant |

**Retorno:**

| Valor | Descrição |
|---|---|
| `true` | Healing target executado |
| `false` | Self-cast rejeitado com POFF |

**Requisitos:**
- Level 300+ (end-game!)
- 400 mana (gigante!)
- Premium account
- Vocação: Druid ou Elder Druid
- Alvo: Friend player específico (NÃO self!)
- Blocking Walls: true

**Exemplo de Uso:**
```lua
// Ao lançar "exura gran sio <friend_name>" (Premium lvl 300+)
-- Validação: var:getNumber() == creature:getId()
-- Se self-cast: rejeita com sendCancelMessage + CONST_ME_POFF!
-- Senão: combat:execute(creature, var)
-- Cura healing ULTRA baseado em fórmula
-- Remove CONDITION_PARALYZE
-- Displays CONST_ME_MAGIC_BLUE effect
-- Cooldown 60s (estratégico!) aplicado
-- Vocações: Druid, Elder Druid
-- NOTE: TibiaWiki says x2 but need more accuracy!
```

---

## 📌 Resumo

**Nature's Embrace** é um spell de healing target premium level 300 druid exclusive END-GAME que oferece healing ULTRA + remoção de paralyze com validação smart de self-cast rejection. Com fórmula dinâmica ULTRA healing (level / 2.5 + maglevel * 20) até (level / 2.5 + maglevel * 28), 400 mana (gigante!), cooldown 60s (estratégico!), spell ID 242, grupo healing, efeito visual CONST_ME_MAGIC_BLUE, sons especiais SOUND_EFFECT_TYPE_SPELL_NATURES_EMBRACE, blocking walls ativado. Target friend name-specific (hasPlayerNameParam!), rejeita self-cast com sendCancelMessage + CONST_ME_POFF, needLearn false. Spell END-GAME ultimate druid target healing premium com smart anti-cheat self-cast validation + TODO formula accuracy note!
