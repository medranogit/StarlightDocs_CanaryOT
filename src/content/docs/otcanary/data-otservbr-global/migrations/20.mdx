---
title: Migration 20 - Market Price Size Fix
description: Muda tipo de price para BIGINT em market_history e market_offers.
sidebar:
  order: 20
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 20  
**Objetivo**: Fix market price field type constraint

## Alterações

```sql
ALTER TABLE `market_history` 
CHANGE `price` `price` BIGINT(20) UNSIGNED NOT NULL DEFAULT '0';

ALTER TABLE `market_offers` 
CHANGE `price` `price` BIGINT(20) UNSIGNED NOT NULL DEFAULT '0';
```

**Tabelas Modificadas**: 
- `market_history`
- `market_offers`

**Alteração**: `price` column → BIGINT(20) UNSIGNED

**Propósito**: Suportar valores maiores de preço no market
