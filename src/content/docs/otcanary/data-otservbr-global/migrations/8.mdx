---
title: Migration 8 - Bestiary Charms
description: Cria tabela player_charms com sistema de charm e runes do bestiary.
sidebar:
  order: 8
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 8  
**Objective**: Create bestiary charm system

## Tabela Criada

```sql
CREATE TABLE `player_charms` (
  `player_guid` INT(250) NOT NULL,
  `charm_points` VARCHAR(250) NULL,
  `charm_expansion` BOOLEAN NULL,
  `rune_wound` INT(250) NULL,
  `rune_enflame` INT(250) NULL,
  ... (20 runes total)
  `UsedRunesBit` VARCHAR(250) NULL,
  `UnlockedRunesBit` VARCHAR(250) NULL,
  `tracker_list` BLOB NULL
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

**Runes**: Wound, Enflame, Poison, Freeze, Zap, Curse, Cripple, Parry, Dodge, Adrenaline, Numb, Cleanse, Bless, Scavenge, Gut, Low Blow, Divine, Vamp, Void

**Propósito**: Armazenar charm points e rune configurations
