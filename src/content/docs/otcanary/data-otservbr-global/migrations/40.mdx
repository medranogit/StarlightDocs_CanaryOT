---
title: Migration 40 - Optimize House Lists
description: Otimiza house_lists com version e PRIMARY KEY.
sidebar:
  order: 40
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 40  
**Objetivo**: Optimize house lists table for performance

## Alterações

```sql
ALTER TABLE `house_lists`
ADD COLUMN `version` bigint NOT NULL DEFAULT 0 AFTER `listid`,
ADD INDEX `version` (`version`),
ADD PRIMARY KEY (`house_id`, `listid`);

ALTER TABLE `house_lists` 
MODIFY `version` bigint(20) NOT NULL DEFAULT '0';
```

**Novas Colunas**:
- `version` - Versão do house list (para tracking de changes)

**Índices**:
- INDEX `version` para queries rápidas
- PRIMARY KEY (`house_id`, `listid`)

**Propósito**: Melhorar performance de queries em house_lists com versionamento
