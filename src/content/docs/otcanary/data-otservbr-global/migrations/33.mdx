---
title: Migration 33 - Add Primary Keys
description: Adiciona PRIMARY KEY a 6 tabelas para melhorar performance.
sidebar:
  order: 33
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 33  
**Objetivo**: Add primary keys for database optimization

## Alterações

```sql
ALTER TABLE `player_prey`
ADD PRIMARY KEY (`player_id`, `slot`);

ALTER TABLE `player_taskhunt`
ADD PRIMARY KEY (`player_id`, `slot`);

ALTER TABLE `player_items`
ADD PRIMARY KEY (`player_id`, `sid`);

ALTER TABLE `player_spells`
ADD PRIMARY KEY (`player_id`, `name`);

ALTER TABLE `player_stash`
ADD PRIMARY KEY (`player_id`, `item_id`);

ALTER TABLE `player_wheeldata`
ADD PRIMARY KEY (`player_id`);
```

**Tabelas Afetadas**: 6 tabelas recebem PRIMARY KEY

**Propósito**: Otimizar queries com índices de chave primária
