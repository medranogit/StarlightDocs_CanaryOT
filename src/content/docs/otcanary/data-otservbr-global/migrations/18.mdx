---
title: Migration 18 - Prey + Task Hunting Rework
description: Restructura prey system e cria task hunting system com múltiplas tabelas.
sidebar:
  order: 18
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 18  
**Objetivo**: Rework prey system and add task hunting

## Alterações players

```sql
ALTER TABLE `players`
DROP `prey_stamina_1`, `prey_stamina_2`, `prey_stamina_3`, 
      `prey_column`,
ADD `prey_wildcard` bigint(21) NOT NULL DEFAULT 0,
ADD `task_points` bigint(21) NOT NULL DEFAULT 0;
```

## Tabelas Removidas

- `player_prey`
- `prey_slots`

## Tabelas Criadas

**player_taskhunt**:
```sql
CREATE TABLE `player_taskhunt` (
  `player_id` int(11) NOT NULL,
  `slot` tinyint(1) NOT NULL,
  `state` tinyint(1) NOT NULL,
  `raceid` varchar(250) NOT NULL,
  `upgrade` tinyint(1) NOT NULL,
  `rarity` tinyint(1) NOT NULL,
  `kills` varchar(250) NOT NULL,
  `disabled_time` bigint(20) NOT NULL,
  `free_reroll` bigint(20) NOT NULL,
  `monster_list` BLOB NULL
);
```

**player_prey** (nova):
```sql
CREATE TABLE `player_prey` (
  `player_id` int(11) NOT NULL,
  `slot` tinyint(1) NOT NULL,
  `state` tinyint(1) NOT NULL,
  `raceid` varchar(250) NOT NULL,
  `option` tinyint(1) NOT NULL,
  `bonus_type` tinyint(1) NOT NULL,
  `bonus_rarity` tinyint(1) NOT NULL,
  `bonus_percentage` varchar(250) NOT NULL,
  `bonus_time` varchar(250) NOT NULL,
  `free_reroll` bigint(20) NOT NULL,
  `monster_list` BLOB NULL
);
```

**Propósito**: Separar prey e task hunting systems
