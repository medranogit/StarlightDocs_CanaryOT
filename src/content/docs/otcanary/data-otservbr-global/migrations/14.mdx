---
title: Migration 14 - Magic Shield Spell
description: Adiciona colunas manashield e max_manashield para suportar spell de magic shield.
sidebar:
  order: 14
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 14  
**Objetivo**: Add magic shield spell support

## Alterações

```sql
ALTER TABLE `players` 
ADD `manashield` SMALLINT UNSIGNED NOT NULL DEFAULT '0' 
    AFTER `skill_manaleech_amount`
ADD `max_manashield` SMALLINT UNSIGNED NOT NULL DEFAULT '0' 
    AFTER `manashield`
```

**Novas Colunas**:
- `manashield` - Current magic shield amount
- `max_manashield` - Maximum magic shield

**Propósito**: Rastrear magic shield absorção
