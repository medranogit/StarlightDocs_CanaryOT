---
title: Migration 27 - Bosstiary System
description: Cria sistema bosstiary com boss_points e tabelas de boosted_boss e player_bosstiary.
sidebar:
  order: 27
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 27  
**Objetivo**: Create bosstiary (boss encyclopedia) system

## Alterações players

```sql
ALTER TABLE `players` 
ADD `boss_points` int NOT NULL DEFAULT '0';
```

## Tabelas Criadas

**boosted_boss**:
```sql
CREATE TABLE `boosted_boss` (
  `boostname` TEXT,
  `date` varchar(250) PRIMARY KEY,
  `raceid` varchar(250),
  `looktype` int DEFAULT 136,
  `lookfeet` int DEFAULT 0,
  `looklegs` int DEFAULT 0,
  `lookhead` int DEFAULT 0,
  `lookbody` int DEFAULT 0,
  `lookaddons` int DEFAULT 0,
  `lookmount` int DEFAULT 0
);
```

**player_bosstiary**:
```sql
CREATE TABLE `player_bosstiary` (
  `player_id` int NOT NULL,
  `bossIdSlotOne` int DEFAULT 0,
  `bossIdSlotTwo` int DEFAULT 0,
  `removeTimes` int DEFAULT 1
);
```

**Propósito**: Rastreamento de boss kills e encyclopedia
