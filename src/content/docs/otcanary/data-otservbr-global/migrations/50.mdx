---
title: Migration 50 - Support to 14.12
description: Refactor completo do charm system para suportar versão 14.12.
sidebar:
  order: 50
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 50  
**Objetivo**: Add support for Tibia 14.12 charm system

## Removem Runes Antigos

```sql
ALTER TABLE `player_charms`
DROP `rune_wound`, `rune_enflame`, ... (19 runes removidas)
```

## Adições Novas

```sql
ALTER TABLE `player_charms`
ADD `minor_charm_echoes` SMALLINT DEFAULT '0',
ADD `max_charm_points` SMALLINT DEFAULT '0',
ADD `max_minor_charm_echoes` SMALLINT DEFAULT '0',
ADD `charms` BLOB NULL
```

## Modificações

```sql
ALTER TABLE `player_charms`
MODIFY `charm_points` SMALLINT DEFAULT '0',
MODIFY `UsedRunesBit` INT DEFAULT '0',
MODIFY `UnlockedRunesBit` INT DEFAULT '0',
CHANGE `player_guid` `player_id` int(11) NOT NULL

ALTER TABLE player_charms
ADD FOREIGN KEY (player_id) REFERENCES players (id)
```

## Data Migration

- Vocations >= 5 recebem: minor_charm_echoes=100, max_minor_charm_echoes=100

**Propósito**: Atualizar para novo charm system (14.12)
