---
title: Migration 32 - Wheel of Destiny
description: Cria tabela player_wheeldata para sistema wheel of destiny.
sidebar:
  order: 32
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 32  
**Objetivo**: Create wheel of destiny system

## Tabela Criada

```sql
CREATE TABLE `player_wheeldata` (
  `player_id` int(11) NOT NULL,
  `slot` blob NOT NULL,
  INDEX `player_id` (`player_id`),
  CONSTRAINT `player_wheeldata_players_fk`
    FOREIGN KEY (`player_id`) REFERENCES `players` (`id`)
    ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8;
```

**Colunas**:
- `player_id` - ID do jogador
- `slot` - BLOB com dados da wheel

**Propósito**: Armazenar configurações do wheel of destiny
