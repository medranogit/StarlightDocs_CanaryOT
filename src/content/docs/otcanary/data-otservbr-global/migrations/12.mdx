---
title: Migration 12 - Boosted Creature Outfit
description: Adiciona colunas de aparência ao boosted_creature.
sidebar:
  order: 12
---

import { Aside, Card } from '@astrojs/starlight/components';

## Visão Geral

**Versão**: 12  
**Objetivo**: Add outfit display for boosted creature

## Alterações

```sql
ALTER TABLE boosted_creature 
ADD `looktype` int(11) NOT NULL DEFAULT 136
ADD `lookfeet` int(11) NOT NULL DEFAULT 0
ADD `looklegs` int(11) NOT NULL DEFAULT 0
ADD `lookhead` int(11) NOT NULL DEFAULT 0
ADD `lookbody` int(11) NOT NULL DEFAULT 0
ADD `lookaddons` int(11) NOT NULL DEFAULT 0
ADD `lookmount` int(11) DEFAULT 0
```

**Novas Colunas**: looktype, lookfeet, looklegs, lookhead, lookbody, lookaddons, lookmount

**Propósito**: Customizar aparência visual da criatura boosted
